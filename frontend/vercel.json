{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "_comment": "Vercel 前端门面。把 /api/* 反代到 Render 后端,使前端相对路径 /api 仍同源(免 CORS、零改前端)。部署:Vercel→New Project→连同一 repo→Root Directory 设为 frontend。把下面 destination 改成 Render 部署后给的真实 URL。",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://ai-interactive-story.onrender.com/api/:path*"
    }
  ]
}
