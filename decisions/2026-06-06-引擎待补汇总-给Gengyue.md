---
date: 2026-06-06
updated: 2026-06-06
status: index
owner: Gengyue (引擎核心决策域)
proposer: Yufei,Claude 整理
---

# 引擎待补汇总(给 Gengyue · 单页全景)

> 把分散在 vault(`卡片体系总览` §四 + 故事书 §6.3)与各 `decisions/` 的引擎待办收成一页,方便主理人 Gengyue 在 repo 内看全。**⚠ Gengyue 看不到 Yufei 的 vault**,故 vault-only 的几条在此显式落地。全属引擎核心 = Gengyue 决策域;Yufei 侧只做内容/前端/确定性解析(`src/parsers.py` 已就位)。
>
> 图例:✅ 已合 main · 📝 提案就绪(待拍板)· 🔧 待补(未提案/进行中)· 🟡 内容侧已绕过、引擎侧仍待

## 一、已合入 main(无需再做)
- ✅ 去 AI 味叙事注入(PR #25,承 `decisions/2026-06-04-去AI味-prompt注入提案`)
- ✅ 对话建卡保全设定卡 visibility + build 支持标 hidden(PR #26,修 #4 剧透 bug)

## 二、提案就绪 · 待主理人拍板(均在本 repo `decisions/`)
- 📝 **角色卡级 hidden / 版本人格注入** — 引擎读 `CharacterData.known_hidden`(注入+门控不说破)+ `known_public` + `versions`(揭穿后覆盖)。现状:引擎只读故事书 `character_boundaries`,卡级字段已落库但 dormant。→ `2026-06-06-角色卡-known_hidden-versions-注入提案`
- 📝 **建卡/读卡确定性 + 补事件卡·设定卡 AI 建卡** — ingest 优先 `detect_kind→parse_card`(确定性、零 LLM),`identify` 仅自由散文兜底;补 events/settingcard 的 `build_card`/`identify`(`_BUILD_SYSTEMS`/`_KINDS` 现仅 4 类);build_card 产物对齐模板格式。→ `2026-06-06-建卡读卡-确定性格式-与事件卡引擎-提案`
- 📝 **建卡前端 best-effort + 引擎待办**(含设定卡 hidden 被剥 bug 的最小修:`_validate_build_draft` worlds 分支保留 visibility/source;stories 分支 events 保留隐藏档字段)→ `2026-06-04-建卡前端best-effort-与引擎待办提案`

## 三、待补全景(卡片体系总览 §四 1–12 + 故事书 §6.3)
> 1–8 / 9–12 是 8 个 subagent 全量建卡实测出的共性:整套卡缺的不是字段,是「条件 / 状态 / 输出控制」这一维的引擎求值。字段侧 `parsers.py` 已能解析落库,等引擎定接入口。

1. 🟡 **设定卡引擎** — 独立 model + 整张解析(现状只能切世界书)。Yufei 侧已用「设定卡摊平成世界书条目并入预设 world」绕过上线(复用现成关键词注入+hidden门控),但 dedicated 设定卡分析引擎仍待补。
2. 📝 **角色卡承接 hidden 真相 + 扩字段** — 隐藏档注入+控登场时机;扩外貌/召回词/边界字段(`parse_character` 已落 anchor/tension/look/keys/versions/known_public/known_hidden,等引擎注入)。见「二」第 1 条。
3. 🔧 **事件卡引擎** — `StoryEvent` 隐藏档:注入(AI 全知)+ 按条件控触发/登场时机 + flag 自动求值;once / cooldown。(`parse_event` 已能读模板,无 build/identify、无运行时求值。)
4. ✅/🔧 **输出层防剧透** — 隐藏 = 全注入 AI + 玩家界面不可见 + AI 不说破/不提前登场;靠 `hard_violation` / CoT 自检 / 披露节奏兜,不靠"不注入"。去AI味部分已合(PR#25);hidden 门控注入的完整闭环仍待(见「二」第 1 条)。
5. 🟡 **完整卡组进预设** — `do_preset` 原只打包 4 类。Yufei 导入器侧已含 世界书 + 14 角色卡 + 演出卡 + 故事书 + 8 城邦设定卡(摊平);**仍缺:隐藏事件卡进预设 + 引擎消费**。
6. 🔧 **per-story system** — 系统提示卡(导演层覆盖)的注入口。
7. 🟡 **演出卡 unknown / opening 字段** — `parse_player` 已解析 unknown(开局不知道)/ opening(开局锚点)并折进 constraints/known_facts;引擎是否单列消费待定。
8. ✅ 去 AI 味(见「一」)。
9. 🔧 **条件求值** — flag/fact 组合(与/或/累计/一票否决)、阈值、变量初值与增减。事件触发、结局谓词(故事书已落 `required_facts`)、关系动态都靠它;现状 flag 不自动求值。
10. 🔧 **状态 / 变量** — 角色私有状态变量、地点状态轴、连续人格漂移(玩家行为累计驱动,非离散切版)。
11. 🔧 **世界书条件维** — anti-keys(命中排除)、组合命中、hidden 解锁翻面、条目关联、注入频率/冷却。
12. 🔧 **揭穿后状态覆盖(reveal override)** — reveal 触发后覆盖角色 speech/人格的运行时支持。见「二」第 1 条(`versions` 已落字段)。

### 故事书 §6.3 新周目机制(本剧本最独特用法,vault-only,在此落地)
- 🔧 **`main_resolved` 单向闩锁回拨** — R3 只锁不回拨;干净重开需:检测到新周目信号(结局达成后玩家续发消息)时回拨 `main_resolved=False` + 清 active 结局态(保留历史 `reached_endings`)。退化方案:闩锁维持 True 不挡续玩,靠 scene/clock/goals 重置 + `fact_周目数` 驱动叙事重开。
- 🔧 **跨周目既视感注入** — 复用 A 档实体轴 + B④ consolidation,高权重 delta 限量跨周目注入,打 `cross_cycle` 标签、收口到白厄/昔涟(普通角色不记得上一轮)。

## 四、Phase 工作(引擎核心,Gengyue 主导)
- 🔧 **Phase 1 记忆可靠性** — 去风险 4 项 PASS;全 IP 对照矩阵在跑,出结果决定合 main(`2026-06-03-phase1-memory-fixes` + `memory-architecture`)。
- 🔧 **Phase 2 实体活档** — 结构化 delta + versioned persona(代码已归档,等评估)。与「二」第 1 条 + §三 10/12 重叠。
- 🔧 **Phase 3 短路径知识边界** — 不在场删召回:长距离 PASS(PR #18);**短路径(近期原文/摘要 scope)未覆盖**。

---
> 内容/前端侧本次已完成(不占 Gengyue):《如我所书》确定性导入上线(parse_character / 演出卡原卡 / 设定卡摊平 / 故事书通用 parse_storybook,零 LLM、零 L4 泄漏)、前端角色详情瘦身。代码见 PR #24。
