.env
.venv/
__pycache__/
*.pyc

# 本地 Claude Code 配置(launch 配置等)——不进库
.claude/

# 运行时 / 用户数据(会话、卡库、故事预设、向量库)——不进库
data/

# 验证脚手架的输出报告 + 临时文件
_validate_report*.json
_babel.js

# 评测平台的运行输出(报告 / playthrough / judge 包)——不进库
eval/runs/

# 版权 IP 的 fixture(如崩坏星穹铁道 canon)——只本地测试用,不进库
eval/fixtures/_*.json

# 内部文档 / 含本机绝对路径的本地脚本(不进公开库)
HANDOFF.md
_smoke_group5a.py
_smoke_group5c.py

# 本机调试 / 试跑产物(scratch, 不进库)
# 注: 不用宽 _* 规则 —— repo 里 _smoke_*/_validate_*/_migrate_*/_seed_preset 等是已跟踪脚手架
*.log
*.zip
_pt_out/
_pt_out_filebased_bak/
_testdata/
_r2*.txt
_canary.txt
_hsr_mid.txt
_libnames.txt
_setcheck.txt
_seed_originals_report.json
# dev server 运行时产物(.err / .pid)
_server_current.*
