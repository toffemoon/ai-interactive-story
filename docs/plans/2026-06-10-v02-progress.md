# v0.2 升级计划 — 执行进度(2026-06-10)

> 对照 [2026-06-10-v02-upgrade-plan.md](2026-06-10-v02-upgrade-plan.md)。
> 本文档记录已完成与未完成项;每完成一批在此打勾并注明 commit。

## ✅ 已完成(分支 fix/v02-batch01)

### 批次 0 · 止血与地基
- **P0-1 RLS**:test 库演练(11 表)→ 回归通过 → prod 应用(11 表)。应用走 postgres owner 直连不受 RLS 影响,Data API 的 anon/authenticated 无 policy 即全拒。迁移文件 `migrations/2026-06-10-enable-rls.sql`。
- **附带发现并修复**:prod 此前缺 phase0 三表(rate_limits/usage_daily/spend_daily),costguard 与新加的认证限流在 prod 全部 fail-open 形同虚设——已按 repo 迁移原样建表(经 MCP apply_migration,含 RLS),并对齐字段精度消除漂移。⚠ costguard 真正生效还需 Render 环境变量 COST_GUARD_ENABLED=1(见 P2-23,等 render.yaml 落定一并做)。
- **P0-4 /oc-assets 收口**:整目录 StaticFiles 挂载 → 白名单路由(仅 jpg/png/webp/gif/mp4/webm),index.json(用户邮箱 PII)与 card.md/profile.md/world.md 不再可拉;带目录穿越防护。
- **P1-2 日志基建**:logging.basicConfig(LOG_LEVEL 可配)+ story_turn 两处吞异常点加 log.exception + memory.py 裸 print 换 logger。
- **P1-7 健康检查(后端侧)**:/api/health 在 DB 不可达时返回 503(status=degraded)。⚠ render.yaml 的 healthCheckPath 仍是 "/",**未改**(本地 render.yaml 有主理人未提交改动,避免冲突;部署侧改探针路径时一并做)。

### 批次 1 · 资损闸门
- **P0-2 LLM 端点统一过闸**:新增 `_guarded_llm`(costguard preflight → 线程池执行 + collect_usage → record 对账),覆盖 identify×4 / identify_auto / build_card / chat;build_card 补鉴权;AUTH 开时全部烧钱端点拒匿名(`_require_login_when_auth`)。
- **P1-3 XFF 修正**:client_ip 改「从右往左取可信跳数」(TRUSTED_PROXY_HOPS,默认 1 = Render 单层反代取末段),伪造首段不再绕过 per-IP 限流。
- **P1-4 登录限流**:/api/auth/login 按 IP(15min/30 次)+ 按 identifier(15min/10 次),新增 costguard.hit_rate 通用闸(fail-open)。
- **P2-11 send_code 限流**:补按 IP(1h/10 次)+ 单邮箱日上限(24h/10 次),原单邮箱 60s 闸保留。

### 批次 2 · 可用性(后端部分)
- **P1-1 同步 LLM 端点 async 化**:identify×4 / identify_auto / build_card / chat 全部 async + to_thread(过渡方案,行为等价),LLM 等待不再占死事件循环。
- **P2-8 upload 上限**:2MB(UPLOAD_MAX_BYTES 可配),Content-Length 预检 + body 实测双闸。
- **P2-9 500 中性化**:11 处 `f"...{e}"` 回显全部改中性文案,细节进服务端日志。

### 批次 2 · 余项(前端,2026-06-11 完成)
- **P2-4 vendor 自托管**:react@18.3.1 / react-dom@18.3.1 / babel-standalone@7.26.4 production 构建入 frontend/vendor/,index.html 三行 src 改本地——unpkg 供应链/可达性风险消除。
- **P2-5 ErrorBoundary**:App 外层包 ErrorBoundary,任何视图渲染抛错(含 window.* 视图脚本缺失)显示可刷新错误卡,不再整页白屏。
- **P2-16 auth/me 重试 + 全局 401**:/api/auth/me 网络/5xx 退避重试 3 次再定论;patchFetch 对带 token 的业务请求统一处理 401(清 token 回登录),/api/auth/* 除外。

### UX 急救清单(8 条 high,2026-06-11 完成;对照 2026-06-10-ux-guidance-review.md)
1. **开屏假承诺**:游客模式/继续游戏(对未登录者全是死路)移除;剩「开始旅程→注册 tab」「登录→登录 tab」两个真实入口,各带白话副标题。
2. **登录卡暗语**:tab 改「登录 · 回到故事」「注册 · 初次到来」,按入口预选;发码提示补垃圾邮件指引。(忘记密码:后端无重置端点,见遗留)
3. **涟漪入局自动开场**:全新一局补水完成仍无回合 → 自动 runTurn 生成开场(只触发一次,续玩不触发),桌面+手机同生效。
4. **创作桌伪 placeholder**:contentEditable 换真 input + placeholder 属性,提示语不再混进用户输入发给 build_card。
5. **游玩页死按钮**:重生成接通 /api/reroll(busy 置灰);存档钮改「已自动存档 · 可随时离开」状态指示;回溯本轮/「雨夜」SUPERVISOR/齿轮/事实边界/记忆档案死 tab/「⚡0」全部隐藏或删除。
6. **新建对话**:真重开当前角色会话(清 sid + 重触发自动开场,旧会话服务端保留);聊天顶栏「记忆/档案」改成实际去向「当前故事/我的」;侧栏「近期聊天」改名「角色」;空态加可点出路。
7. **等待反馈**:聊天角色侧动效 pending 气泡「TA 正在落笔…」、创作「执笔人推演中…」行、游玩「叙事推演中▋」;busy 时发送钮置灰、文案变化、回车不再静默吞;聊天/创作新消息自动滚到底。
8. **手机键盘**:viewport 加 interactive-widget=resizes-content(Chrome)+ MShell visualViewport 压缩壳高兜底 iOS;全部 Enter 提交点补 isComposing 守卫(中文组词回车不再误提交);不可聊角色输入框禁用并给指引;移动创作假「存草稿」移除;事件卡 tab(误映射 characters)移除。

验证:后端 _validate_accounts 41/41;CDP 无头实测桌面+移动 27/27、AUTH-on 开屏 9/9,无 JS 报错;「新建对话」闭环实测两次开场内容不同。

### 批次 4 · DB 卫生 + UX high 余项(2026-06-11 完成,分支 fix/v02-batch04)
- **P2-18 迁移基线**:prod schema 经 MCP 实查汇编入 `migrations/0000-baseline-prod-schema.sql`;此后 DDL 一律走 apply_migration。
- **P3-4 复合索引**:sessions (user_id, updated_at desc),旧单列索引删除。
- **P2-19 PK + 原子 upsert**:cards/presets 加 identity PK;save_library/save_preset 改 on conflict 对准部分唯一索引(单语句原子),根治并发首存 500。
- **P2-21 FK**:sessions/cards/presets/memory_vec 四列 user_id 补 FK(on delete set null,NOT VALID→VALIDATE,prod 全过)。
- **P2-20 孤儿向量**:59 行先备份进 `_backup_memory_vec_orphans_20260611`(含 embedding,RLS 开)再清理;delete_session 同事务补删向量。⚠ **不加** memory_vec.session_id 级联 FK——引擎先写向量后建 sessions 行,该 FK 会让新局首回合失败(此为 P2-20 标引擎核心的原因;delete_session 改动只动删除路径,请主理人过目)。
- **P2-22 resolve_token 瘦身**:热路径 SELECT 去 avatar(17KB/请求)+ last_used_at 节流(>5min);/api/auth/me 低频单独补 avatar。
- **P3-2 过期清理**:启动时批量删过期 token(30d)/otp(7d)/rate_limits(2d),fail-open。
- **P2-26**:DB_POOL_MAX 已 env 化(db.py 原有),数值核算与 render.yaml 固化待部署侧。
- **忘记密码全栈**:/api/auth/reset_password(验证码 purpose=reset 分用途 + 重置后吊销全部 token)+ 登录卡「忘记密码?」重置模式。
- **UX high 余项**:游客存档并入账号(登录时一次性询问);「仅本机」角标+说明(桌面+手机);0 回合占位档不再注册+列表过滤;presets//my/sessions 失败态改「加载失败·重试」(不再伪装空态,保留上次缓存);ReconHome 未登录死按钮移除;建卡→使用闭环(角色卡入库直通聊天 + 聊天列表并入自有卡库角色)。

验证:test 库迁移演练(幂等×2)→ 回归 41/41 → prod apply_migration(6 约束 VALIDATE/索引/59 行备份清理全核验);重置密码 API 冒烟 10/10;CDP 前端 10/10 无 JS 报错。

## ⏳ 未完成(按计划批次)

### UX 余项(结构性,后续批)
- **会话持久化**(聊天 byKey 提升 App 层/localStorage,复用 sid 续聊)/ **coach 重接** / **历史回合回看** / 六格统计去重 / 选角后果说明等 medium 项:见 UX 报告第二、三节。

### 批次 3 · 引擎核心批(⚠ 全部需主理人压测后合 main)
- **P0-3 回合并发锁**(pg_advisory_xact_lock 包 load→save;同 session 并发 409)。
- **P1-5 chat 历史落库**(expand-contract 双写:dict + sessions/messages 表)。
- **P1-6 reroll 瘦快照**(快照只存 message_count,回滚按 seq 截断)。
- P2-12 摘要异步化 / P2-13 long_memory 封顶 / P2-14 向量限时降级。

### 批次 4 · DB 卫生(趁数据量小,先于增长)
- **P2-18 迁移基线**(pg_dump --schema-only 入库 0000-baseline——**先行于其余 DB 项**)。
- P3-4 sessions (user_id, updated_at desc) 复合索引;P2-19 cards/presets 加 PK;P2-21 user_id 四列补 FK(NOT VALID→VALIDATE);P2-20 memory_vec 孤儿清理(59 行);P2-22 resolve_token 去 avatar+节流写;P3-2 token/otp/rate_limits 过期清理;P2-23 render.yaml env 固化(等本地改动落定);P2-26 DB 池核算。

### 批次 5 · 性能与产品面
- P2-2 tail 增量化;P2-1 turns 拆表(expand-contract,周期最长);P2-27 列表分页;P2-6 服务端权威卡组(+P3-8);P2-7/P2-10 存档归属收口(73 个无主局批量认领→关过渡期放行);P2-15(暂缓:自动开场是主理人点名要的功能,体验优先)/P2-17 前端会话治理。

### 批次 6 · 前端工程化与扫尾
- P2-24 预编译(babel-standalone 下线);P2-3 CSP(先 Report-Only);Vite 评估;P3 其余(P3-1/3/5/6/7/9/10/11/12)。

## 备注
- P0-2 行为变化:**AUTH 开(prod)下,识别/建卡/聊天均需登录**;游客模式只能浏览与玩故事(story_turn 仍按原闸)。前端聊天页对 401 的呈现待 P2-16 一并优化。
- 本批全部改动不碰引擎核心(story/memory 仅加日志);可独立回滚(回滚点见 commit)。
